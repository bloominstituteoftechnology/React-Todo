import React from "react";

const TodoForm = props => (
  <form>
    <input type="" placeholder="...todo" />
    <button>Add ToDo</button>
    <button>Clear Completed</button>
  </form>
);
export default TodoForm;
