import React from 'react';
import {FlexColumn} from "../Elements/Flex";


const Todo = props => {
    return (
        <div>
            {props.children}
        </div>
    );
};

export default Todo;
