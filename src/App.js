import React from 'react';
import { render } from 'react-dom';
import TodoList from './Components/TodoList';

const App = () => (
  <div>
    <h2>Todo App</h2>
    <TodoList />
  </div>
);

export default App;
