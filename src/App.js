import React from 'react';

class TodoList extends React.Component {
  state = {
    todos: [
      'text',
      'textext',
    ],
  }
}

const App = () => (
  <div>
    <h2>Todo App</h2>

  </div>
);

export default App;
